# Data locations for test_driver.launch.py. Empty => repo-relative defaults
# (<repo>/network_dump for input pcaps, <repo>/bags for output bags), both
# gitignored. Set absolute paths here to override, or export $PINGDSP_REPO.
network_dump_dir: ''
bag_dir: ''
